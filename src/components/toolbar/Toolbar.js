import React from 'react';
import MuiToolbar from '@material-ui/core/Toolbar';

/**
 * A wrapper of Material UI Toolbar
 */
export function Toolbar(props) {
  return <MuiToolbar {...props} />;
}
